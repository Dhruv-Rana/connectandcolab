const listTalkHandler = require('./listTalk')
const sendTalkHandler = require('./sendTalk')
const getTalkHandler = require('./getTalk')

module.exports = {
  listTalkHandler,
  sendTalkHandler,
  getTalkHandler,
}
