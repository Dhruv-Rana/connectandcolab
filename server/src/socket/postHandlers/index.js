const listPostHandler = require('./listPost')
const getPostHandler = require('./getPost')
const createPostHandler = require('./createPost')

const addCommentPostHandler = require('./addComment')

module.exports = {
  listPostHandler,
  getPostHandler,
  createPostHandler,
  addCommentPostHandler,
}
